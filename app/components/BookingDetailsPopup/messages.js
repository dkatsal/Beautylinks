/*
 * BookingDetailsPopup Messages
 *
 * This contains all the text for the BookingDetailsPopup component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.BookingDetailsPopup';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BookingDetailsPopup component!',
  },
});
