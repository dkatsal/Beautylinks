/*
 * BookingPopup Messages
 *
 * This contains all the text for the BookingPopup component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.BookingPopup';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BookingPopup component!',
  },
});
