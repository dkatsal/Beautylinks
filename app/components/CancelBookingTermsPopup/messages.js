/*
 * CancelBookingTermsPopup Messages
 *
 * This contains all the text for the CancelBookingTermsPopup component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CancelBookingTermsPopup';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CancelBookingTermsPopup component!',
  },
});
