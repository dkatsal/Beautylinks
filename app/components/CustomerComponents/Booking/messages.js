/*
 * Booking Messages
 *
 * This contains all the text for the Booking component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Booking';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Booking component!',
  },
});
