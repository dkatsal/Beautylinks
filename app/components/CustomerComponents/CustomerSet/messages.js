/*
 * CustomerSet Messages
 *
 * This contains all the text for the CustomerSet component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CustomerSet';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CustomerSet component!',
  },
});
