/*
 * CustomerSidebar Messages
 *
 * This contains all the text for the CustomerSidebar component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CustomerSidebar';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CustomerSidebar component!',
  },
});
