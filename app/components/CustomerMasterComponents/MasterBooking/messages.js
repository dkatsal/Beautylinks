/*
 * MasterBooking Messages
 *
 * This contains all the text for the MasterBooking component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MasterBooking';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MasterBooking component!',
  },
});
