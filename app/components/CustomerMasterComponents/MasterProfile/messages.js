/*
 * MasterProfile Messages
 *
 * This contains all the text for the MasterProfile component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MasterProfile';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MasterProfile component!',
  },
});
