/*
 * MasterServices Messages
 *
 * This contains all the text for the MasterServices component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MasterServices';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MasterServices component!',
  },
});
