/*
 * MasterSettings Messages
 *
 * This contains all the text for the MasterSettings component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MasterSettings';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MasterSettings component!',
  },
});
