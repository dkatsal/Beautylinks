/*
 * MasterSidebar Messages
 *
 * This contains all the text for the MasterSidebar component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MasterSidebar';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MasterSidebar component!',
  },
});
