/*
 * FaqClient Messages
 *
 * This contains all the text for the FaqClient component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.FaqClient';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FaqClient component!',
  },
});
