/*
 * HeaderTop Messages
 *
 * This contains all the text for the HeaderTop component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.HeaderTop';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HeaderTop component!',
  },
});
