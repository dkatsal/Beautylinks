/*
 * JustBooked Messages
 *
 * This contains all the text for the JustBooked component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.JustBooked';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the JustBooked component!',
  },
});
