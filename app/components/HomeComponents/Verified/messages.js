/*
 * Verified Messages
 *
 * This contains all the text for the Verified component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Verified';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Verified component!',
  },
});
