/*
 * MasterPageBooking Messages
 *
 * This contains all the text for the MasterPageBooking component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MasterPageBooking';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MasterPageBooking component!',
  },
});
