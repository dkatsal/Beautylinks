/*
 * MasterPageGallery Messages
 *
 * This contains all the text for the MasterPageGallery component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MasterPageGallery';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MasterPageGallery component!',
  },
});
