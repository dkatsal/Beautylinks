/*
 * MasterPageSchedule Messages
 *
 * This contains all the text for the MasterPageSchedule component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MasterPageSchedule';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MasterPageSchedule component!',
  },
});
