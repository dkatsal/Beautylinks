/*
 * ReCaptcha
 *
 * This contains all the text for the Recaptcha component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ReCaptcha';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ReCaptcha component!',
  },
});
