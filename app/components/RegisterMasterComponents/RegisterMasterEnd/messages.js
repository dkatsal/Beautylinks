/*
 * RegisterMasterEnd Messages
 *
 * This contains all the text for the RegisterMasterEnd component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.RegisterMasterEnd';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RegisterMasterEnd component!',
  },
});
