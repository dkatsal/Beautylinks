/*
 * RegisterMasterForm Messages
 *
 * This contains all the text for the RegisterMasterForm component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.RegisterMasterForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RegisterMasterForm component!',
  },
});
