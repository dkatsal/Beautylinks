/*
 * RegisterMasterLocation Messages
 *
 * This contains all the text for the RegisterMasterLocation component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.RegisterMasterLocation';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RegisterMasterLocation component!',
  },
});
