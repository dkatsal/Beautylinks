/*
 * RegisterMasterServices Messages
 *
 * This contains all the text for the RegisterMasterServices component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.RegisterMasterServices';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RegisterMasterServices component!',
  },
});
