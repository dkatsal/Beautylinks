/*
 * RegisterMasterSkills Messages
 *
 * This contains all the text for the RegisterMasterSkills component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.RegisterMasterSkills';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RegisterMasterSkills component!',
  },
});
