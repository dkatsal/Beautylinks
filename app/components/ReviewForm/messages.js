/*
 * ReviewForm Messages
 *
 * This contains all the text for the ReviewForm component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ReviewForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ReviewForm component!',
  },
});
