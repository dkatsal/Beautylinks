/*
 * CookiesPolicy Messages
 *
 * This contains all the text for the CookiesPolicy container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CookiesPolicy';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CookiesPolicy container!',
  },
});
