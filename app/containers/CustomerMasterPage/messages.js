/*
 * CustomerMasterPage Messages
 *
 * This contains all the text for the CustomerMasterPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CustomerMasterPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CustomerMasterPage container!',
  },
});
