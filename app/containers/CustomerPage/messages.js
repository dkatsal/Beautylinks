/*
 * CustomerPage Messages
 *
 * This contains all the text for the CustomerPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CustomerPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CustomerPage container!',
  },
});
