/*
 * CustomerProfile Messages
 *
 * This contains all the text for the CustomerProfile container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CustomerProfile';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CustomerProfile container!',
  },
});
