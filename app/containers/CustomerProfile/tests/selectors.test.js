// import { selectCustomerProfileDomain } from '../selectors';

describe('selectCustomerProfileDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
