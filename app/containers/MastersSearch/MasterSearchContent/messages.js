/*
 * PractitionerSearch Messages
 *
 * This contains all the text for the PractitionerSearch container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PractitionerSearch';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PractitionerSearch container!',
  },
});
