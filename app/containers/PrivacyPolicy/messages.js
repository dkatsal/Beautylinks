/*
 * PrivacyPolicy Messages
 *
 * This contains all the text for the PrivacyPolicy container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PrivacyPolicy';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PrivacyPolicy container!',
  },
});
