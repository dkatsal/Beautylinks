/*
 * RegisterMainPage Messages
 *
 * This contains all the text for the RegisterMainPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RegisterMainPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RegisterMainPage container!',
  },
});
