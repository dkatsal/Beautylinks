/*
 * RegisterMasterPage Messages
 *
 * This contains all the text for the RegisterMasterPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RegisterMasterPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RegisterMasterPage container!',
  },
});
