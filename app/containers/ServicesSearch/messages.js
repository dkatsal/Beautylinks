/*
 * ServicesSearch Messages
 *
 * This contains all the text for the ServicesSearch container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ServicesSearch';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ServicesSearch container!',
  },
});
